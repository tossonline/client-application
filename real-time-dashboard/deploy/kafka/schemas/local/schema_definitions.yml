apiVersion: 1
spec:
  schemas:
    - topic: EVT_DOSCPT_IGBAFFILIATES_DEV_AAP_EXT_<CONTEXT-NAME>_<SERVICE-NAME>_<EVENT-NAME>
      schema: value
      schema.file.type: avro
      schema.file.location: deploy/kafka/schemas/<AVRO FILE NAME>.avsc
      schema.compatibility.level: forward